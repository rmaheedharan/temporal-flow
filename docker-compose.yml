services:
  temporal:
    image: temporalio/temporal:latest
    command: server start-dev --ip 0.0.0.0
    ports:
      - "7233:7233"   # gRPC — app connects here
      - "8233:8233"   # Web UI — http://localhost:8233
